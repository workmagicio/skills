```
# Ad Performance {Cadence} Report · {date_range}

> **Period covered:** {start_date} – {end_date}  ·  **Comparison:** vs prior {cadence} ({prior_start} – {prior_end})
> **Tenant:** {tenant_name}  ·  **Attribution model:** {model_name}

---

## 📊 Core Metrics

| Channel    | Spend     | Revenue   | ROAS    | CTR   | CPA    | NC ROAS | WoW (ROAS) |
|------------|-----------|-----------|---------|-------|--------|---------|------------|
| Meta       | ${m_spend}| ${m_rev}  | {m_roas}| {m_ctr}%| ${m_cpa}| {m_ncroas}| {m_wow}%   |
| Google     | ${g_spend}| ${g_rev}  | {g_roas}| {g_ctr}%| ${g_cpa}| {g_ncroas}| {g_wow}%   |
| TikTok     | ${t_spend}| ${t_rev}  | {t_roas}| {t_ctr}%| ${t_cpa}| {t_ncroas}| {t_wow}%   |
| **Total**  | **${tot_spend}** | **${tot_rev}** | **{tot_roas}** | —     | —      | **{tot_ncroas}** | **{tot_wow}%** |

---

## 🟢 Highlights ({this period})

(Auto-include rows where WoW delta ≥ +20% on a primary metric. Cap at 2.)

- **{channel} ROAS reached {value}** this {cadence} (+{pct}% WoW), driven by {top_campaign / top_tactic}.
- **{channel} new-customer orders +{pct}% WoW** — likely from {audience_change / creative_refresh}.

If no row qualifies: *"No standout positive movement this {cadence} — performance is steady across channels."*

---

## 🔴 Issues & Risks

(Auto-include rows where CPA exceeds target OR ROAS fell below threshold OR WoW delta ≤ −20%. Cap at 3.)

- **{channel}** CPA was **${cpa}** ({pct}% over target) — review audience quality / creative fatigue.
- **{channel} ROAS dropped from {prev} to {curr}** (−{pct}% WoW) — see {anomaly_diagnosis_link} for diagnosis.
- **{tactic} spend grew {pct}% but orders flat** — possible click dilution; check UTM health.

If no row qualifies: *"No material risk surfaced this {cadence}."*

---

## 💡 Next {cadence} Recommendations

(2-3 concrete actions, each ≤ 1 sentence. Tactical, not strategic — strategic recs belong in executive variant.)

1. **{Channel}**: {specific action} — expected impact: {outcome}.
2. **{Channel}**: {specific action} — expected impact: {outcome}.
3. **{Channel}**: {specific action} — expected impact: {outcome}.

---

*Data as of: {query_time}*  ·  *Attribution model: {model_name}*  ·  *Generated by Justin AI*

📊 [Open in WorkMagic dashboard]({dashboard_url})  ·  📥 [Download as CSV]({csv_url})  ·  ⚙️ [Edit this scheduled report]({task_url})

```

**How to fill**:

- {cadence} = "weekly" / "daily" / "monthly"
- {date_range} = "Jun 17 – Jun 23, 2026" (for in-app / Slack) or ISO range
- {model_name} = "iDDA" / "DDA" (whatever the tenant default resolved to)
- Replace {m\_\*}, {g\_\*}, {t\_\*} blocks with one row per channel **actually configured** for the tenant — don't include channels with 0 spend that week unless explicitly requested
- Highlight emoji 🟢 / 🔴 / 💡 must use exactly those — clients have learned to scan by color
- If a channel has CPA target or ROAS threshold configured in the tenant settings, use those; otherwise omit the "over target" language
- The 3 footer links are required (dashboard / CSV / edit scheduled task) — clients use the edit link 10x more than they use chat
